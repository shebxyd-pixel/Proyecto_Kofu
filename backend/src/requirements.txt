# --- Núcleo (obligatorias) ---
fastapi
uvicorn[standard]
pydantic
requests

# --- Investigación web (fallback si no hay Google API) ---
beautifulsoup4

# --- Generación de Office ---
python-pptx
python-docx

# --- Opcionales (el backend degrada sin crashear si faltan) ---
python-dotenv       # carga .env con OLLAMA_MODEL, GOOGLE_API_KEY, etc.
bleach               # sanitización HTML más estricta (si falta, usa fallback con regex)
markitdown           # /files: conversión de archivos a texto (si falta, ese endpoint se desactiva)
pywin32; platform_system == "Windows"   # plantillas .potx/.dotx (solo Windows + Office instalado)
